import React from "react";

const ScheduleGrid = () => {
  return (
    <section className="cr-schedule-grid">
      {/* Weekly grid, drag-and-drop, shift blocks, conflict highlights */}
    </section>
  );
};

export default ScheduleGrid;
