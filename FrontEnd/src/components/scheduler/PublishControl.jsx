import React from "react";

const PublishControl = () => {
  return (
    <div className="cr-publish-control">
      {/* Publish/unpublish controls, versioning */}
      <button>Publish</button>
      <button>Unlock for Edits</button>
    </div>
  );
};

export default PublishControl;
